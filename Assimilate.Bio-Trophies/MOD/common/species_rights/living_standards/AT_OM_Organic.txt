LivingStandard_AT_AR_OM_TrophyMerge = {
	# intentionally no happiness bonus - gene-therapy is uncomfortable
	# duplicates pop_modifier from living_standard_organic_trophy

	pop_group_modifier = { pop_category_bio_trophy_consumer_goods_upkeep_add = @living_standard_luxuries_very_high }
	potential = {
		from? = {
			has_valid_civic = civic_machine_servitor
			has_technology = tech_gene_tailoring
		}
	}
	allow = {
		hidden_trigger = { exists = from }
		custom_tooltip = {
			fail_text = species_is_not_trophy
			has_citizenship_type = {
				type = citizenship_organic_trophy
				country = from
			}
		}
		custom_tooltip = {
			fail_text = species_at_least_one_primary_bio_trophy
			from = {
				any_owned_species = {
					NOT = { is_same_value = root }
					has_citizenship_type = {
						type = citizenship_organic_trophy
						country = from
					}
					NOT = {
						has_living_standard = {
							type = LivingStandard_AT_AR_OM_TrophyMerge
							country = from
						}
					}
				}
			}
		}
	}
	ai_will_do = { factor = 0 }
}
